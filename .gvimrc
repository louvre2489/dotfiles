" 縦幅
set lines=1920
" 横幅
set columns=1020

" 色
syntax on
syntax enable
set background=dark
colorscheme material-theme
highlight Cursor guibg=Green
highlight Visual guibg=darkmagenta

" 透明度
" for windows
"autocmd GUIEnter * set transparency=240
"autocmd FocusGained * set transparency=240
"autocmd FocusLost * set transparency=160
" for ubuntu
" with 'sudo apt-get install compizconfig-settings-manager compiz-plugins'
